---
import { Main } from '@/main'
import { RedirectIfLoggedIn } from '@/components/redirectIfLoggedIn'
import Layout from '@/layout.astro'
---

<Layout>
    <Main />
    <RedirectIfLoggedIn client:only="react" />
</Layout>
